@model mci_main.Models.SearchResults

@{
    ViewData["Title"] = "Search Results";
}

<h1>Search</h1>

<p>
    <h1>Results</h1>
</p>
@if (Model.Practitioners != null && Model.Practitioners.Any())
{
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Practitioners[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Practitioners[0].Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Practitioners[0].Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Practitioners[0].Bio)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Practitioners)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Title)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Location)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Bio)
                </td>
                <td>
                    <a asp-action="Details" asp-route-id="@item.MciIdx">Details</a> |
                </td>
            </tr>
            }
    </tbody>
</table>
}
else
{
    <h2>No Results, sorry</h2>
    <p>Try again here: </p>
}
